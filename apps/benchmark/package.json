{
  "name": "zoon-benchmark",
  "version": "1.0.0",
  "scripts": {
    "benchmark:tokens": "bun run src/token-benchmark.ts",
    "benchmark:accuracy": "bun run src/accuracy-gpt.ts",
    "benchmark:quick": "bun run src/accuracy-quick.ts",
    "benchmark": "bun run benchmark:tokens && bun run benchmark:accuracy"
  },
  "dependencies": {
    "@toon-format/toon": "^2.1.0",
    "@zoon-format/zoon": "workspace:*",
    "js-tiktoken": "^1.0.21",
    "openai": "^6.15.0",
    "zon-format": "^1.3.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
